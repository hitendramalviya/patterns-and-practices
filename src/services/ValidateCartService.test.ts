// Validate method should be called with valid URL path "/validation/cart" POST

// Validate method should be supplying data to be posted in expected format { cartId: id, validateQty: true, validatePrice: true }
